/**
 * App Mixin
 *
 * Import new mixins here and add them to the `mixins` array.
 * This makes their features available to all components using this mixin.
 *
 * Example:
 * import i18nMixin from './i18n-mixin';
 * export default {
 *   mixins: [i18nMixin],
 * }
 */

import i18nMixin from "./i18n-mixin";

export default {
    mixins: [i18nMixin],
}
